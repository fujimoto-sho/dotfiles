# Review Loop

Claude Code と Codex を連携したレビューサイクルを実行します。

以下のコマンドを実行してください:

```bash
~/.claude/commands/review-loop.sh $ARGUMENTS
```

## 使用例
- `/review-loop "ユーザー認証機能を実装"`
- `/review-loop -m 3 "バグ修正"`
- `/review-loop -f "src/auth.ts" "リファクタリング"`
